// forEach
// display students details li
const students = [
  { name: "Alice", age: 20 },
  { name: "Bob", age: 22 },
  { name: "Charlie", age: 21 },
];

students.forEach((student) => {
  console.log(`${student.name} is ${student.age}`);
});
